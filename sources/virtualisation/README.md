# Sources — cours de virtualisation (début)

Notes de Jean-Mathieu Chantrein, inspirées de la playlist KVM de xavki
(lien en fin de `notes.md`) : concepts, pile virsh → libvirt → QEMU →
KVM, installation, première VM en CLI (`virt-install`/`virsh`), memo de
commandes.

Destination : série « Virtualisation avec KVM » développée sur cette
trame (voir `../../docs/plan-integration-outils-admin-sys.md`, § 10.2 ;
erreurs relevées au § 11).
