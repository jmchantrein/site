---
title: "Le masquage d'adresse IP"
description: "Comprendre les masques IP (CIDR) : un simulateur graphique interactif des bits réseau/hôte."
order: 5
topics: [reseau]
duration: 15
badge: "Fondations"
tags: [CIDR, IPv4, masque]
terminal: false
---

## 1. Réseau ou hôte : la frontière

Une adresse IPv4 fait 32 bits. Le **préfixe** CIDR (le `/24` de `192.168.1.0/24`) indique combien de bits, en partant de la gauche, désignent le *réseau*. Les bits restants désignent l'*hôte* à l'intérieur de ce réseau.

<Note title="Le masque">
  Le **masque** est l'adresse où les bits réseau valent 1 et les bits hôte valent 0. `/24` ⇒ `255.255.255.0`.
</Note>

<Note type="info" title="Évolution · des classes au CIDR">
  - **Avant.** L'espace IPv4 était découpé en **classes** figées : A (`/8`), B (`/16`), C (`/24`). La taille du réseau était imposée par les premiers bits de l'adresse.
  - **Le problème.** Gaspillage massif (une classe B = 65 534 hôtes, souvent pour quelques machines) et **explosion des tables de routage** ; l'épuisement des adresses se profilait.
  - **Aujourd'hui.** Le **CIDR** (*Classless Inter-Domain Routing*, 1993) rend le préfixe **libre** (`/p` quelconque) : découpage fin (VLSM) et **agrégation de routes** (*supernetting*). C'est le modèle décrit ici.
  - **Pourquoi c'est mieux.** Allocation au plus juste, tables de routage compactes — et l'on a gagné du temps avant le passage à IPv6.
</Note>

<Bypass title="Simulateur de masque IP">
  <IpSim />
</Bypass>

## 2. Ce que le masque détermine

- **Adresse réseau** : bits hôte tous à 0 — l'identité du réseau.
- **Diffusion** : bits hôte tous à 1 — atteint tous les hôtes.
- **Hôtes utilisables** : $2^{(32-p)} - 2$ (on retire réseau et diffusion).

<Note type="warning" title="Cas limites">
  `/31` n'a pas de diffusion (2 hôtes, liaison point-à-point, RFC 3021) et `/32` désigne un seul hôte.
</Note>

<Slides title="Diaporama — à retenir">
  <Slide>
    ### Préfixe = frontière

    Le `/p` fixe le nombre de bits réseau. Plus `p` est grand, plus le réseau est petit (moins d'hôtes).
  </Slide>
  <Slide>
    ### Réseau & diffusion

    Hôtes à 0 ⇒ adresse réseau ; hôtes à 1 ⇒ diffusion. Ni l'une ni l'autre n'est attribuable à une machine.
  </Slide>
  <Slide>
    ### Compter les hôtes

    $2^{(32-p)} - 2$. Exemple : `/26` ⇒ $2^{6}-2 = 62$ hôtes.
  </Slide>
</Slides>
