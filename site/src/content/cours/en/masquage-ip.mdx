---
title: "IP address masking"
description: "Understanding IP subnet masks (CIDR): an interactive graphical simulator of network/host bits."
order: 5
topics: [reseau]
duration: 15
badge: "Foundations"
tags: [CIDR, IPv4, subnet mask]
terminal: false
provenance:
  by: human
  reviewedBy: ai
  model: "Claude Opus 4.x"
  translated: ai
---

## 1. Network or host: the boundary

An IPv4 address is 32 bits long. The CIDR **prefix** (the `/24` in `192.168.1.0/24`) indicates how many bits, starting from the left, identify the *network*. The remaining bits identify the *host* within that network.

<Note title="The subnet mask">
  The **subnet mask** is the address whose network bits are set to 1 and whose host bits are set to 0. `/24` ⇒ `255.255.255.0`.
</Note>

<Note type="info" title="Evolution · from classes to CIDR">
  - **Before.** The IPv4 space was carved into fixed **classes**: A (`/8`), B (`/16`), C (`/24`). The network size was dictated by the address's leading bits.
  - **The problem.** Massive waste (one class B = 65,534 hosts, often for a handful of machines) and a **routing-table explosion**; address exhaustion loomed.
  - **Today.** **CIDR** (*Classless Inter-Domain Routing*, 1993) makes the prefix **free** (any `/p`): fine-grained subnetting (VLSM) and **route aggregation** (*supernetting*). This is the model described here.
  - **Why it is better.** Tighter allocation, compact routing tables — and it bought time before the move to IPv6.
</Note>

<Bypass title="IP subnet mask simulator">
  <IpSim />
</Bypass>

## 2. What the mask determines

- **Network address**: all host bits set to 0 — the identity of the network.
- **Broadcast**: all host bits set to 1 — reaches every host.
- **Usable hosts**: $2^{(32-p)} - 2$ (subtracting the network and broadcast addresses).

<Note type="warning" title="Edge cases">
  `/31` has no broadcast address (2 hosts, point-to-point link, RFC 3021) and `/32` designates a single host.
</Note>

<Slides title="Slideshow — key takeaways">
  <Slide>
    ### Prefix = boundary

    The `/p` sets the number of network bits. The larger `p` is, the smaller the network (fewer hosts).
  </Slide>
  <Slide>
    ### Network & broadcast

    Host bits at 0 ⇒ network address; host bits at 1 ⇒ broadcast. Neither can be assigned to a machine.
  </Slide>
  <Slide>
    ### Counting hosts

    $2^{(32-p)} - 2$. Example: `/26` ⇒ $2^{6}-2 = 62$ hosts.
  </Slide>
</Slides>
