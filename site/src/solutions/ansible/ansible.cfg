# /etc/ansible/ansible.cfg — the control node's configuration.
# One duty here: say where the inventory lives (a directory: every file
# inside is read). interpreter_python silences the Python-discovery notice.
[defaults]
inventory=/etc/ansible/inventory
interpreter_python=auto_silent
