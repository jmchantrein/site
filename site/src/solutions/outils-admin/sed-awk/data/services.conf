# Managed services configuration (workshop sample file).
[web]
port = 8080
user = www-data
autostart = yes

[base_de_donnees]
port = 5432
user = postgres
autostart = yes

[sauvegarde]
port = 0
user = backup
autostart = no
